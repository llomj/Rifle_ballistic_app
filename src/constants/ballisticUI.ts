/**
 * Shared ballistic UI constants.
 * If the user requests a size change for any circle, update this value and all circles will match.
 */
export const CIRCLE_SIZE_PX = 314; // Compass / calculation circles (40% bigger than 224px base)

/** Fixed height for circle slot — same on main and calculate page. Circle never moves; panel moves below. */
export const CIRCLE_SLOT_HEIGHT = '55vh';
